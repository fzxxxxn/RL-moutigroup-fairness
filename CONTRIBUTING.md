# Contributing

Thanks for your interest! Please open an issue before large changes. Use feature branches + PRs.
- Run `pytest -q` before submitting.
- Keep interfaces stable; add new plug-ins via `registry` instead of modifying core.
- Follow `black` + `isort` + `ruff` style.
